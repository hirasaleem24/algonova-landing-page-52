import { motion } from "framer-motion";

<motion.section
  id="demo"
  initial={{ opacity: 0, scale: 0.9 }}
  whileInView={{ opacity: 1, scale: 1 }}
  transition={{ duration: 0.8 }}
  viewport={{ once: true }}
  className="py-24 bg-black/70 backdrop-blur-lg flex flex-col items-center justify-center"
>
  {/* content here */}
</motion.section>
